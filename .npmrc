# Obsidian community-plugin convention: git tags are bare versions
# (e.g. `0.0.1`), NOT `v0.0.1`. Drop the prefix `npm version` adds by default
# so the tag matches manifest.version exactly. Required by the release
# workflow's tag↔manifest.version check.
tag-version-prefix=""
